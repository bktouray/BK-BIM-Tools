<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="BK BIM Tools - Floors to Footings"
    Width="440"
    SizeToContent="Height"
    MinWidth="400" MinHeight="380"
    WindowStartupLocation="CenterScreen"
    ResizeMode="CanResizeWithGrip"
    Background="{StaticResource Surface.Base}"
    FontFamily="Segoe UI">

    <!-- Branded replacement for Floors to Footings' remaining default-pyRevit
         prompts (family/material forms.SelectFromList, auto-size/delete-source
         forms.alert yes/no choices). Color brushes come from the shared
         ui/themes/Tokens.xaml, merged in code-behind - see bkbim/ui/tokens.py. -->
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="Caption">
            <Setter Property="Foreground" Value="{StaticResource Text.Secondary}"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource Text.Primary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="0,0,0,4"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Text.Primary}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Background" Value="{StaticResource Surface.Raised}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border.Default}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource Text.Primary}"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Background" Value="{StaticResource Brand.Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="BK BIM TOOLS" Style="{StaticResource Caption}" FontWeight="SemiBold"/>
            <TextBlock Text="Floors to Footings" FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource Text.Primary}" Margin="0,2,0,4"/>
            <TextBlock x:Name="SubtitleText" Style="{StaticResource Caption}" TextWrapping="Wrap"/>
        </StackPanel>

        <Separator Grid.Row="1" Margin="0,16,0,16"/>

        <!-- Base footing family -->
        <StackPanel Grid.Row="2">
            <Label Content="Base footing family"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="FamilyText" Grid.Column="0" Text="No family chosen yet"
                           VerticalAlignment="Center" TextWrapping="Wrap"
                           Foreground="{StaticResource Text.Primary}"/>
                <Button x:Name="ChooseFamilyButton" Grid.Column="1" Content="Choose…" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
            </Grid>
        </StackPanel>

        <!-- Auto-size -->
        <StackPanel Grid.Row="3" Margin="0,16,0,0">
            <CheckBox x:Name="AutoSizeCheckBox" Content="Auto-generate a footing type sized to each floor (recommended)"/>
        </StackPanel>

        <!-- Material -->
        <StackPanel Grid.Row="4" Margin="0,12,0,0">
            <Label Content="Material for all footings"/>
            <ComboBox x:Name="MaterialCombo" Height="28"/>
        </StackPanel>

        <!-- Delete source -->
        <StackPanel Grid.Row="5" Margin="0,16,0,0">
            <CheckBox x:Name="DeleteSourceCheckBox" Content="Delete the original floors after creating footings (recommended)"/>
        </StackPanel>

        <!-- Validation message -->
        <TextBlock x:Name="ValidationText" Grid.Row="6" Style="{StaticResource Caption}"
                   TextWrapping="Wrap" Margin="0,10,0,0" Visibility="Collapsed"/>

        <!-- Row 7 ("*") intentionally empty: absorbs extra vertical space so the
             footer stays anchored near the bottom instead of floating mid-window
             when the user resizes taller. -->

        <!-- Footer -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelButton" Content="Cancel" IsCancel="True" Style="{StaticResource SecondaryButton}" Margin="0"/>
            <Button x:Name="RunButton" Content="Run" IsDefault="True" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
