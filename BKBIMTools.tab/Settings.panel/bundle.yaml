# Settings panel (PRD §4). One button opens the whole Settings window
# (Settings platform design) - Office Standards is the first real page;
# General/About/placeholder categories land inside the same window, not as
# separate ribbon buttons - see docs/architecture (Settings platform design).
layout:
  - Settings
